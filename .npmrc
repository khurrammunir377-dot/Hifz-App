# Use hoisted node_modules layout so that Metro bundler and Expo can
# resolve packages the same way they expect with a flat node_modules tree.
# This is the recommended setting for Expo / React Native projects with pnpm.
node-linker=hoisted
